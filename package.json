{
  "name": "@kysmith/page-object-selectors",
  "version": "1.0.0",
  "description": "Small testing library which acts as an interface between your tests and the DOM",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "kyle.smith.m@gmail.com",
  "license": "ISC",
  "scripts": {
    "start": "concurrently --kill-others --names tsc,wds \"npm run tsc:watch\" \"wds --node-resolve --open\"",
    "tsc:watch": "tsc --watch",
    "build": "tsc"
  },
  "devDependencies": {
    "@web/dev-server": "^0.0.25",
    "concurrently": "^5.3.0",
    "typescript": "^4.1.2"
  }
}
